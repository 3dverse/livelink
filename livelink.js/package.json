{
  "name": "@3dverse/livelink",
  "version": "0.2.2",
  "description": "",
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "typings": "./dist/sources/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "node ci/auto-generate.js ../ftl-schemas/ ci/ ./_prebuild/types/",
    "build": "node esbuild.js && tsc",
    "dev": "node esbuild.js dev & tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "docs": "typedoc",
    "postversion": "cross-env-shell \"git commit -am v$npm_package_version && git tag -a v$npm_package_version -m v$npm_package_version\""
  },
  "license": "ISC",
  "dependencies": {
    "@3dverse/livelink.core": "^0.2.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.20.1",
    "rimraf": "^5.0.7",
    "typedoc": "^0.25.13",
    "typedoc-plugin-markdown": "^4.0.3",
    "typescript": "^5.4.5"
  }
}
