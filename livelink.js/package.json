{
  "name": "@3dverse/livelink",
  "version": "0.2.2",
  "description": "",
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "typings": "./dist/sources/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "node ci/auto-generate.js ../node_modules/ ci/ ./_prebuild/types/",
    "build": "node esbuild.js && tsc",
    "dev": "run-p dev:*",
    "dev:esbuild": "node esbuild.js dev",
    "dev:tsc": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "docs": "typedoc",
    "postversion": "cross-env-shell \"git commit -am v$npm_package_version && git tag -a v$npm_package_version -m v$npm_package_version\""
  },
  "license": "ISC",
  "dependencies": {
    "@3dverse/livelink.core": "^0.2.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.20.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.7",
    "typedoc": "^0.25.13",
    "typedoc-plugin-markdown": "^4.0.3",
    "typescript": "^5.4.5"
  }
}
