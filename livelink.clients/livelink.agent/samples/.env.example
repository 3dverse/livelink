# Copy to `.env` (gitignored) and fill in. Both samples read that file through
# `--env-file-if-exists`, so exporting the variable in your shell works just as well.

# A 3dverse access token with rights on the scene the sample drives.
LIVELINK_TOKEN=
LIVELINK_API_KEY=