{
  "name": "livelink.test",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build:core": "npm -C ../livelink.core run build && npm run build:js",
    "build:js": "npm -C ../livelink.js run build && npm run build",
    "build": "node esbuild.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/wicg-file-system-access": "^2023.10.4",
    "esbuild": "^0.20.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "livelink.js": "file:../livelink.js"
  }
}
